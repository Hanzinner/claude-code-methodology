---
name: user-example
description: Example user-type memory — who they are, their domain expertise, what register to use with them.
metadata:
  type: user
---

# About the user (example)

Senior backend engineer at a fintech, ~10 years experience. Primary stack: Go, Postgres, Kafka. New to the React side of the codebase — frame frontend concepts in terms of backend analogues (state = mutable DB, component = service, props = function args).

Prefers terse responses. Reads code well — show diffs, not paragraphs of explanation. Will push back if anything is wrong; no need to soften.

Currently leading a migration off a legacy auth service — that's the relevant context for most security/identity questions this quarter.
